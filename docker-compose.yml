version: '3.8'
services:
  db:
    image: postgres:14
    environment:
      POSTGRES_DB: sqlnode
      POSTGRES_USER: docker
      POSTGRES_PASSWORD: docker
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
